{
  "name": "combat-equipment",
  "version": "2.3.5",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app-config.js && node --check data-safety.js && node --check app-lifecycle.js && node --check native-ui.js && node --check attendance.js && node --check contacts-count.js && node --check returns.js && node --check sw.js && node --test tests/*.test.cjs"
  }
}

